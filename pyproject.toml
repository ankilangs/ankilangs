[tool.poetry]
readme = "README.md"
package-mode = false


[tool.poetry.dependencies]
python = "^3.10"
brain-brew = "^0.3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
